# This file is for Cloudflare Pages, not Workers
# Pages will automatically deploy from GitHub without this config

# Build configuration for Pages
[build]
command = "npm run build"
cwd = "."

[env.production]
vars = { NODE_ENV = "production" }
